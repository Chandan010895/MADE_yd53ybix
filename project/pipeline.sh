#!/bin/bash

# A script to run the Python data pipeline

# Activate the virtual environment (if using one)
# source /path/to/venv/bin/activate

# Run the Python script to start the data pipeline
python3 /path/to/your/project/pipeline.py
